import 'package:flutter/material.dart';

class AppRadii {
  static const token1 = BorderRadius.all(Radius.circular(4));

  static const wrike = BorderRadius.all(Radius.circular(4));

  static const newOne = BorderRadius.all(Radius.circular(4));

  static const token4 = BorderRadius.all(Radius.circular(4));

  AppRadii._();
}
